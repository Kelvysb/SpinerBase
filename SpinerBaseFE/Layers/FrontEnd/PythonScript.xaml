<Window x:Class="SpinerBase.Layers.FrontEnd.PythonScript"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpinerBase.Layers.FrontEnd"
       mc:Ignorable="d" Name="PythonScriptEditor"
      Title="SpinerBase - Python" Height="500" Width="450" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackColor}" Icon="/SpinerBase;component/Icon.ico" ShowInTaskbar="False" WindowStyle="ToolWindow" Loaded="PythonScript_Loaded" Topmost="True">

    <Grid Name="grdPrincipal">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Image Name="imgBackground" Margin="0" Grid.ColumnSpan="2" Grid.RowSpan="3" Source="/SpinerBase;component/Resources/SpinerBaseBackground.jpg" HorizontalAlignment="Left" VerticalAlignment="Stretch" Stretch="UniformToFill" Width="450"/>
        
        <TextBox Name="txtCommand" Text="" Style="{StaticResource stlTextBoxBase}" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5" Grid.Row="0"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="65"/>
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource ButtonRoundRectangleStyle}" Margin="5" Grid.Column="0">
                <TextBlock Style="{StaticResource stlTextBlockBase}" Margin="5">
                            Create an function called main with a single string parameter 
                            <LineBreak/>
                            that returns a string, Example: def main(input): return "Hello"                             
                </TextBlock>
            </Border>
            <Button Name="btnok" Style="{StaticResource ButtonRoundStyle}" Margin="0,3,10,2" Grid.Column="1" HorizontalAlignment="Right" ToolTip="Back" Click="btnok_Click">
                <Grid>
                    <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Style="{StaticResource ButtonRoundRectangleStyle}"/>
                    <Image Source="/SpinerBase;component/Resources/Save.png"/>
                </Grid>
            </Button>

        </Grid>

    </Grid>
</Window>

